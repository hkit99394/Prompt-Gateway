# Staging environment variable values (copy to staging.tfvars and fill in)
# Usage: terraform plan -var-file=staging.tfvars
# NEVER commit staging.tfvars - it may contain sensitive values

aws_region  = "us-east-1"
environment = "staging"
